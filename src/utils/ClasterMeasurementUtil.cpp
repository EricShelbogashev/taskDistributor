#include "ClasterMeasurementUtil.h"

ClasterMeasurementUtil::ClasterMeasurementUtil(size_t processNumber) : _processNumber(processNumber) {}

std::vector<int> ClasterMeasurementUtil::calculateSizes(const Matrix &matrix) const {
    return std::vector<int>();
}

std::vector<int> ClasterMeasurementUtil::calculateDisplacements(const Matrix &matrix) const {
    return std::vector<int>();
}
